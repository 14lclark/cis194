{-# OPTIONS_GHC -Wall #-}

toDigits    :: Integer -> [Integer]
toDigitsRev :: Integer -> [Integer]

toDigits = undefined

toDigitsRev a
  | a < 10 = [a]
  | otherwise = (digit : (toDigits next))
  where
    digit = a `mod` 10
    next = (a - digit) `div` 10
